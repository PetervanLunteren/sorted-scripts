##### HASH PASSWORDS
# make sure to replace the actual password in the .hash('xxxx')
# You can safely ignore the 'Thread 'MainThread'' warnings 

pip install streamlit_authenticator

python -c "import streamlit_authenticator as stauth; print(stauth.Hasher().hash('SillySeal8!'))"
